name: GL
on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: setup-env
        run: |
          sudo apt install cloc
          for d in ./*/ ; do (cd "$d" && echo "$d" && cloc --vcs git); done
